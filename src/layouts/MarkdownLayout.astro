---
import Layout from './Layout.astro';

const { title, lang, activeNav, alternates, noindex } = Astro.props.frontmatter;
---

<Layout title={title} lang={lang} activeNav={activeNav} alternates={alternates} noindex={noindex}>
  <slot />
</Layout>
